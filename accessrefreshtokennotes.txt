Concept of refresh and Access token(session token):

#why?
--->safety 
--->login method easy 

#what? 
When a user first time login in a website/app, using their/user details we create 
Two types of token : 1. Access token, 2. Refresh token 
Access token : expire time short
Refresh token: expire time long



#How ? 

process ---> 
1. when a user first login in a website/app we create 
this two token using jwt (json web token)

2. we store them in cookies 

3. we store refresh tokens in database



userdatails+ (extra )  --->   fsadgwsgfwqtqewrqretewrtwerrywert
                        jwt

using jwt,(json web token)
Using cryptographic algorithums
There are three parts in jwt 
1.Header 
2.payload(our data which we want to make token) 
3.verify Signeture(there are secret key which make token unique)


the refresh token to 
The database ; and backend gives Access and Refresh token in the form of cokkies to 
The user

When user  have access token, he can authenticate , but when their access token 
Expire,  user can give their refresh token to backend and backend match the refresh 
token (user) with refresh token (database)


 if it match , then backend give permission to user to authenticate and backend 
Generate new access token and refresh token and give the user
